@{ ViewData["Title"] = "Your profile!"; }

<div class="row">
    <div class="col-sm-3"></div>
    <div class="col-sm-6">
        <div class="registration-card">
            <img class="logo" src="~/images/match-logo-img.png" alt="company logo">
            @if(ViewBag.statuspage=="height")
            {
            <div id="heightSlider"></div>
            <button class="height-btn btn btn-success">"This is how tall I am?"</button>
            }
            @if(ViewBag.statuspage=="weight")
            {
            <label for='weight'>Weight:</label>
            <input type='text' name='weight'/>
            <button class="height-btn btn btn-success">"How much do I weight?"</button>
            }
            @if(ViewBag.statuspage=="smoke")
            {

            }
            @if(ViewBag.statuspage=="drink")
            {
            <label for='smoke'>'Do I drink?'</label>
            <select name='drink'>
                <option value='No'>Male</option>
                <option value='Yes'>Female</option>
            </select>
            <!-- <label for="dealbreak">Is this a dealbreak</label>
            <input type="radio" id="Yes" name="dealbreak" value="Yes">
            <label for="No">Female</label>
            <input type="radio" id="No" name="dealbreak" value="No"> -->
            }
        </div>
        <div class="form-group">
        <!-- <label for="height" class="col-sm-2 control-label">height</label>
        <div class="col-sm-5">
            <select name="foot"class="form-control" id="foot">
                <option value="0">Foot</option>
                @{
                    for(var i = 1; i <= 6; i++){
                        <option value="@i">@i</option>
                    }
                }  
            </select>
        </div> -->
        <div id='profileform' style="display: none;">
            
        </div>
    </div>
    <div class="col-sm-3"></div>
</div>